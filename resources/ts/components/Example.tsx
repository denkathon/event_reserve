import React from 'react';

const Example: React.FC = () => {
    return (
        <div className="container">
            <h1>Hello, TypeScript in Laravel!</h1>
        </div>
    );
};

export default Example;
